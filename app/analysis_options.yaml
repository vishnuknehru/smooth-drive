include: package:flutter_lints/flutter.yaml

plugins:
  riverpod_lint: ^3.1.4

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  rules:
    - prefer_single_quotes
    - always_declare_return_types
    - unawaited_futures
    - directives_ordering
